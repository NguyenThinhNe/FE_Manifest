# Persistent Volume Claim
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: portfolio-pvc
# spec:
#   accessModes:
#     - ReadWriteMany
#   storageClassName: longhorn
#   resources:
#     requests:
#       storage: 1Gi

---
# Deployment Object
apiVersion: apps/v1
kind: Deployment
metadata:
  name: warranty-management-fe-deployment-prod
spec:
  selector:
    matchLabels:
      app: warranty-management-fe-prod
  replicas: 2
  template:
    metadata:
      labels:
        app: warranty-management-fe-prod
    spec:
      containers:
      - name: warranty-management-fe-prod
        image: fleeforezz/warranty-management-fe:1.1-beta
        resources:
          limits:
            cpu: "1000m"
            memory: "1000Mi"
          requests:
            cpu: "500m"
            memory: "750Mi"
        # volumeMounts:
        # - mountPath: "/"
        #   name: portfolio-storage
        ports:
        - containerPort: 3000
      # volumes:
      # - name: portfolio-storage
      #   persistentVolumeClaim:
      #     claimName: portfolio-pvc
      automountServiceAccountToken: false

---

# Service Object
apiVersion: v1
kind: Service
metadata:
  name: warranty-management-fe-service-prod
spec:
  selector:
    app: warranty-management-fe-prod
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 32525
