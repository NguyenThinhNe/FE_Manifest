# Persistent Volume Claim
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: portfolio-pvc
# spec:
#   accessModes:
#     - ReadWriteMany
#   storageClassName: longhorn
#   resources:
#     requests:
#       storage: 1Gi

---
# Deployment Object
apiVersion: apps/v1
kind: Deployment
metadata:
  name: warranty-management-fe-deployment-dev
spec:
  selector:
    matchLabels:
      app: warranty-management-fe-dev
  replicas: 2
  template:
    metadata:
      labels:
        app: warranty-management-fe-dev
    spec:
      containers:
      - name: warranty-management-fe-dev
        image: fleeforezz/warranty-management-fe:1.13-beta
        resources:
          limits:
            cpu: "1000m"
            memory: "1000Mi"
          requests:
            cpu: "500m"
            memory: "750Mi"
        # volumeMounts:
        # - mountPath: "/"
        #   name: portfolio-storage
        ports:
        - containerPort: 5173
      # volumes:
      # - name: portfolio-storage
      #   persistentVolumeClaim:
      #     claimName: portfolio-pvc
      automountServiceAccountToken: false

---

# Service Object
apiVersion: v1
kind: Service
metadata:
  name: warranty-management-fe-service-dev
spec:
  selector:
    app: warranty-management-fe-dev
  type: NodePort
  ports:
    - port: 5173
      targetPort: 5173
      nodePort: 32524
